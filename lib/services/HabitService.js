const User = require('../models/User');
const Habit = require('../models/Habit');


module.exports = class HabitService {
    // .create({ phoneNumber, habitImageURL })
    //findUserByPhone(phoneNumber)
    //Habit.insert({ habitImageURL, userId })
    //detectLabels(habitImageURL)
    //sendSms(phoneNumber, 'Thanks! Which of these is the best label for your photo?....')
}
